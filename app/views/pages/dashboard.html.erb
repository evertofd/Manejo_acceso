<p id="notice"><%= notice %></p>

<h1>Usuarios</h1>

<table class="table table-striped table-hover ">
  <thead>
    <tr>
      <th>Usuario</th>
      <th>Rol</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @user.each do |u| %>
    <tr>
      <td><%= u.name %></td>
      <td><%= u.role %></td>
      <td>


    
            <%= link_to 'Eliminar User', destroy_user_path(u), method: :delete, class: 'btn btn-danger',data: { confirm: 'Estás seguro que deseas eliminar al usuario?' } %> </td>

          <td></td>

        </tr>
        <% end %>
      </tbody>
    </table>

    <br>

    <%= link_to 'Nuevo Post', new_post_path, class: 'btn btn-primary' %>
